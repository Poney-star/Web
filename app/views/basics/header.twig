<input type="checkbox" id="nav-toggle" class="nav-toggle">
<label for="nav-toggle" class="nav-toggle-label">
    <span></span>
    <span></span>
    <span></span>
</label>

<nav>
    <div class="logo-container">
        <img src="img/blossom-removebg-preview.png" alt="LeBonPlan Logo">
    <ul>
        <li><a {{ app.request.query.get('page') == 'home' ? 'class = "active"' : 'href="?page=home"' }}>Accueil</a></li>
        <li><a {{ app.request.query.get('page') == 'offers' ? 'class = "active"' : 'href="?page=offers"' }}>Offre de stage</a></li>
        <li><a {{ app.request.query.get('page') =='companies' ? 'class = "active"' : 'href="?page=companies"' }}>Entreprises</a></li>
        <li><a {{ app.request.query.get('page') == 'users' ? 'class = "active"' : 'href="?page=users"' }}>Utilisateurs</a></li>
        <li><a {{ app.request.query.get('page') == 'contact' ? 'class = "active"' : 'href="?page=contact"' }}>Nous contacter</a></li>
    </ul>
    </div>
    <a href="{{ loggedIn ? '?page=profile' : '?page=login' }}" class="login-btn">
        {{ loggedIn ? 'Mon Compte' : 'Se connecter / S\'inscrire' }}
    </a>
</nav>

