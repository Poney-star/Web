{% extends "basics/layout.twig" %}

{% block content %}
<main class="container main-content">

  <div class="content-wrapper">
    <!-- Sidebar -->
    <aside class="sidebar">
      <div class="sort-section">
        <span class="sort-label">Trier par :</span>
        <div class="select-wrapper">
          <select class="sort-select">
            <option>Mieux notés</option>
            <option>Plus récentes</option>
            <option>Ordre alphabétique</option>
          </select>
          <span class="select-icon">▼</span>
        </div>
      </div>
      
      <div class="filter-header">
        <span class="filter-title">Filtres</span>
        <span class="filter-icon">⚙️</span>
      </div>
        
        <div class="accordion">
          <button class="accordion-btn">
            <span>Localisation</span>
            <span class="accordion-icon">▼</span>
          </button>
          <div class="accordion-content">
            {% for filterElement in filters %}
              {% if filterElement.Ville is not empty %}
                <label class="checkbox-label">
                  <input type="checkbox" class="filter-checkbox city"> {{ filterElement.Ville }}
                </label>
              {% endif %}
            {% endfor %}
          </div>
        </div>
        
        <div class="accordion">
          <button class="accordion-btn">
            <span>Durée de stage</span>
            <span class="accordion-icon">▼</span>
          </button>
          <div class="accordion-content">
            {% for filterElement in filters %}
              {% if filterElement.Duree is not empty %}
                <label class="checkbox-label">
                  <input type="checkbox" class="filter-checkbox duration"> {{ filterElement.Duree }}
                </label>
              {% endif %}
            {% endfor %}
          </div>
        </div>
        
        <div class="accordion">
          <button class="accordion-btn">
            <span>Secteur d'activité</span>
            <span class="accordion-icon">▼</span>
          </button>
          <div class="accordion-content">
            {% for filterElement in filters %}
              {% if filterElement.Secteur_Activite is not empty %}
                <label class="checkbox-label">
                  <input type="checkbox" class="filter-checkbox sector"> {{ filterElement.Secteur_Activite }}
                </label>
              {% endif %}
            {% endfor %}
          </div>
        </div>
        
        <div class="accordion">
          <button class="accordion-btn">
            <span>Entreprise</span>
            <span class="accordion-icon">▼</span>
          </button>
          <div class="accordion-content">
            {% for filterElement in filters %}
              {% if filterElement.Nom is not empty %}
                <label class="checkbox-label">
                  <input type="checkbox" class="filter-checkbox company"> {{ filterElement.Nom }}
                </label>
              {% endif %}
            {% endfor %}
        </div>
      </div>
      <button class="btn filter-confirm">Appliquer les filtres</button>
    </aside>

    <!-- Main Content -->
    <div class="content">
      <div class="page-header">
        <h2 class="page-title">NOS OFFRES DE STAGE</h2>
        <p class="page-subtitle">
          Trouvez le stage qui vous convient parmi plus de 1,5 millions présents sur ce site
        </p>
      </div>

      <!-- Search Bar -->
      <div class="search-container">
        <div class="search-box">
          <span class="search-icon">🔍</span>
          <input type="text" placeholder="Entrez un mot clé" class="search-input">
        </div>
        <div class="search-box">
          <span class="search-icon">📍</span>
          <input type="text" placeholder="Rechercher par ville" class="search-input">
        </div>
      </div>

      <!-- Internship Cards -->
      <div class="cards-grid">
        {% include "components/offers.twig" %}
      </div>
      
      <!-- Pagination -->
      <div class="pagination">
        <p>{{app.request.query.get('l') ~ '/' ~ pages}}</p>
      </div>
    </div>
  </div>
</main>
{% endblock %}

{% block stylesheets %}
<link rel="stylesheet" href="css/offer.css">
{% endblock %}

{% block scripts %}
<script src="js/offer.js"></script>
{% endblock %}

{% block title %}
Offres - Career Bloom
{% endblock %}